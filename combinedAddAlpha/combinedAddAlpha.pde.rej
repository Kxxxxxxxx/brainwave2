diff a/combinedAddAlpha/combinedAddAlpha.pde b/combinedAddAlpha/combinedAddAlpha.pde	(rejected hunks)
@@ -141,8 +141,16 @@
            t1.gameNumber -= 1;
          }
        } else if (keyCode == ENTER) {
+           bgmTitle.pause();
+           if (t1.gameNumber == 1){
+              bgmEndurance.loop();
+           }
+           if (t1.gameNumber == 2){
+              bgmBalloon.loop();
+           }
            if (t1.gameNumber == 3){
               changeWindowSize(640, 480);
+              bgmNonomura.loop();           
            }
            state = t1.gameNumber;
          }
